-- Ensure the script is running in the right context (Client)
if not game:IsLoaded() then
    game.Loaded:Wait()
end

-- Create the ScreenGui for the player
local player = game.Players.LocalPlayer
local screenGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
screenGui.Name = "RattHatterV4Gui"
screenGui.ResetOnSpawn = false

-- Create the main frame
local mainFrame = Instance.new("Frame", screenGui)
mainFrame.Size = UDim2.new(0, 500, 0, 600)
mainFrame.Position = UDim2.new(0.5, -250, 0.5, -300)
mainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
mainFrame.BorderSizePixel = 0
mainFrame.Active = true
mainFrame.Draggable = true

-- Add UIGradient
local frameGradient = Instance.new("UIGradient", mainFrame)
frameGradient.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 0, 0)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(50, 0, 0))
})
frameGradient.Rotation = 45

-- Add texture effect
local texture = Instance.new("ImageLabel", mainFrame)
texture.Size = UDim2.new(1, 0, 1, 0)
texture.Position = UDim2.new(0, 0, 0, 0)
texture.BackgroundTransparency = 1
texture.Image = "rbxassetid://123456789"  -- Replace with your desired texture ID
texture.ImageTransparency = 0.5

-- Blur effect (if you want it)
local blur = Instance.new("BlurEffect", game.Lighting)
blur.Size = 4

-- Create the title label
local titleLabel = Instance.new("TextLabel", mainFrame)
titleLabel.Size = UDim2.new(1, 0, 0, 50)
titleLabel.Position = UDim2.new(0, 0, 0, 10)
titleLabel.BackgroundTransparency = 1
titleLabel.Text = "🐀 Ratt Hatter"
titleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLabel.TextScaled = true
titleLabel.Font = Enum.Font.GothamBold
titleLabel.TextStrokeTransparency = 0.5
titleLabel.TextStrokeColor3 = Color3.fromRGB(200, 0, 0)

-- Create the close button
local closeButton = Instance.new("TextButton", mainFrame)
closeButton.Size = UDim2.new(0, 40, 0, 40)
closeButton.Position = UDim2.new(1, -50, 0, 5)
closeButton.Text = "X"
closeButton.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.Font = Enum.Font.GothamBold
closeButton.TextScaled = true
closeButton.BorderSizePixel = 0

-- Close functionality
closeButton.MouseButton1Click:Connect(function()
    screenGui:Destroy()
end)

-- Create the minimize button
local minimizeButton = Instance.new("TextButton", mainFrame)
minimizeButton.Size = UDim2.new(0, 40, 0, 40)
minimizeButton.Position = UDim2.new(1, -100, 0, 5)
minimizeButton.Text = "-"
minimizeButton.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
minimizeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
minimizeButton.Font = Enum.Font.GothamBold
minimizeButton.TextScaled = true
minimizeButton.BorderSizePixel = 0

-- Minimize functionality
local minimized = false
minimizeButton.MouseButton1Click:Connect(function()
    if not minimized then
        mainFrame.Size = UDim2.new(0, 0, 0, 50)
        minimized = true
    else
        mainFrame.Size = UDim2.new(0, 500, 0, 600)
        minimized = false
    end
end)

-- Create the scrolling frame for buttons
local scrollFrame = Instance.new("ScrollingFrame", mainFrame)
scrollFrame.Size = UDim2.new(1, -20, 0, 500)
scrollFrame.Position = UDim2.new(0, 10, 0, 70)
scrollFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
scrollFrame.ScrollBarThickness = 10
scrollFrame.BorderSizePixel = 0

-- Layout for buttons
local layout = Instance.new("UIGridLayout", scrollFrame)
layout.CellSize = UDim2.new(0, 120, 0, 50)
layout.CellPadding = UDim2.new(0, 5, 0, 5)

-- Function to add buttons
local function addButton(text, scriptUrl)
    local button = Instance.new("TextButton", scrollFrame)
    button.Size = UDim2.new(0, 120, 0, 50)
    button.Text = text
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
    button.Font = Enum.Font.Gotham
    button.TextScaled = true

    -- Button hover effects
    button.MouseEnter:Connect(function()
        button.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
    end)
    button.MouseLeave:Connect(function()
        button.BackgroundColor3 = Color3.fromRGB(80, 0, 0)
    end)

    -- Button click functionality
    button.MouseButton1Click:Connect(function()
        loadstring(game:HttpGet(scriptUrl))()
    end)
end

-- Add buttons with scripts
addButton("Chat Bypass #1", "https://pastefy.app/CJ0E33tA/raw")
addButton("Chat Bypass #2", "https://raw.githubusercontent.com/BakaPraselol/MRCBV4LSB4KRS/main/Loader")
addButton("Chat Bypass #3", "https://pastefy.app/r39aZhXJ/raw")
addButton("Chat Bypass #4", "https://raw.githubusercontent.com/Synergy-Networks/products/main/BetterBypasser/loader.lua")
addButton("Bang Inf Yield #1", "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source")

